package app.pausecn.usage

/** Shared by initial setup and the later heatmap controls. Neither grants AI upload permission. */
const val USAGE_LOCAL_CONSENT = "只为所选目标保存日/小时统计，从启用并授权后开始，不导入之前的历史。需要你另行授予系统使用情况访问；读取到的原始事件只在内存计算，不保存其他应用记录，也不发送给DeepSeek。你可以暂停采集或清历史。"
